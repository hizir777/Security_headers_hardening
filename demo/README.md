# Demo

Replace this directory's placeholder content with your final demo
artefacts before submission.

## Video walk-through

Replace the YouTube URL below with the link to your screen-recorded
demo:

[Watch the demo](https://www.youtube.com/watch?v=PLACEHOLDER_REPLACE_ME)

The demo should cover:

1. `git clone` and `npm install`.
2. `npm run dev`.
3. Hitting `curl -sI http://127.0.0.1:3000/` and pointing out each
   security header in the response.
4. Opening <http://127.0.0.1:3000/audit> and clicking *Run audit*.
5. Running `npm test` to show the Jest assertions pass.
6. Building the Docker image and showing `docker compose up`.
7. Pointing the SecurityHeaders.com scanner at the public deploy and
   showing the A+ grade.

## Screenshots

Drop annotated screenshots here:

- `01-curl-headers.png`
- `02-audit-ui.png`
- `03-jest-pass.png`
- `04-securityheaders-grade.png`
- `05-observatory-grade.png`
